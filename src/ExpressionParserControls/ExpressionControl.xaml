<UserControl x:Class="ExpressionParserControls.ExpressionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpressionParserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}}">
        <RichTextBox x:Name="ExpressionTextBox" TextChanged="ExpressionTextBox_TextChanged" SelectionChanged="ExpressionTextBox_SelectionChanged" AcceptsReturn="False" AcceptsTab="False">
            <RichTextBox.Resources>
                <Style TargetType="Hyperlink">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="Red" />
                            <Setter Property="Cursor" Value="Hand" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="False">
                            <Setter Property="Foreground" Value="Blue" />
                        </Trigger>
                    </Style.Triggers>
                    <EventSetter Event="MouseLeftButtonDown" Handler="HyperlinkMouseLeftButtonDown"/>
                </Style>
            </RichTextBox.Resources>
            <FlowDocument>
                <!--The following paragraph is for visualization purposes only and will get removed on text changed.-->
                <Paragraph>
                    <Run x:Name="ExpressionRun" Text="{Binding Path=Text}" />
                </Paragraph>
            </FlowDocument>
        </RichTextBox>
    </Grid>
</UserControl>
