<UserControl x:Class="GenericControls.NumericTextBox2" x:Name="NumericText"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GenericControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <TextBox 
            Name="NumericTBox" 
            VerticalAlignment="Stretch" 
            HorizontalAlignment="Stretch" 
            VerticalContentAlignment="Center" 
            PreviewKeyDown="TextBox_PreviewKeyDown" 
            PreviewTextInput="TextBox_PreviewTextInput" 
            PreviewKeyUp="TextBox_PreviewKeyUp"
            Text="{Binding Value, ElementName=NumericText, UpdateSourceTrigger=LostFocus, Mode=TwoWay, StringFormat='G'}">
        </TextBox>

        <Border HorizontalAlignment="Stretch" Height="{Binding ElementName=NumericTBox, Path=ActualHeight}" IsHitTestVisible="False" BorderThickness="1">
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=NumericText, Path=ValueIsValid}" Value="False">
                            <Setter Property="BorderBrush" Value="Red"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=NumericText, Path=ValueIsValid}" Value="True">
                            <Setter Property="BorderBrush" Value="{x:Null}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>

    </Grid>
</UserControl>
