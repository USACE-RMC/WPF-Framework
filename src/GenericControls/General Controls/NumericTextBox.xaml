<UserControl x:Class="GenericControls.NumericTextBox" x:Name="NumericTextBoxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBox x:Name="NumericTBox" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" PreviewKeyDown="TextBox_PreviewKeyDown" PreviewTextInput="TextBox_PreviewTextInput" TextChanged="TextBox_TextChanged" Text="{Binding ElementName=NumericTextBoxControl, Path=Text}" IsReadOnly="{Binding ElementName=NumericTextBoxControl, Path=IsReadOnly}">
            <TextBox.Style>
                <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=NumericTextBoxControl, Path=AboveMaxValue}" Value="True">
                            <Setter Property="ToolTip" Value="Value is above maximum allowed value"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=NumericTextBoxControl, Path=BelowMinValue}" Value="True">
                            <Setter Property="ToolTip" Value="Value is below minimum allowed value"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=NumericTextBoxControl, Path=InvalidText}" Value="True">
                            <Setter Property="ToolTip" Value="Text entered is not a number"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=NumericTextBoxControl, Path=ValueIsValid}" Value="True">
                            <Setter Property="ToolTip" Value="{x:Null}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
        <Border HorizontalAlignment="Stretch" Height="{Binding ElementName=NumericTBox, Path=ActualHeight}" IsHitTestVisible="False" BorderThickness="1">
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=NumericTextBoxControl, Path=ValueIsValid}" Value="False">
                            <Setter Property="BorderBrush" Value="Red"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=NumericTextBoxControl, Path=ValueIsValid}" Value="True">
                            <Setter Property="BorderBrush" Value="{x:Null}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
    </Grid>
</UserControl>
