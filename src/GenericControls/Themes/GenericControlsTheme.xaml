<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:GenericControls">

    <!--
    GenericControls Theme Resource Dictionary

    This dictionary contains theme-aware styles for GenericControls that use
    DynamicResource bindings to respond to runtime theme changes.

    All colors are referenced from the Themes library using DynamicResource
    so they automatically update when the theme is changed.
    -->

    <!-- Merge Icon Dictionary for centralized vector icons -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/GenericControls;component/Resources/IconDictionary.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- COMMON STYLES                                                                -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <!-- Separator Background (transparent) -->
    <SolidColorBrush x:Key="SeparatorBackground">#00FFFFFF</SolidColorBrush>

    <!-- Dot Leader Line Style for property controls -->
    <Style TargetType="{x:Type Line}" x:Key="DotLeaderLineStyle">
        <Setter Property="Grid.Column" Value="1"/>
        <Setter Property="X1" Value="0"/>
        <Setter Property="Y1" Value="0"/>
        <Setter Property="X2" Value="1"/>
        <Setter Property="Y2" Value="0"/>
        <Setter Property="Stroke" Value="{DynamicResource Button.Static.Border}"/>
        <Setter Property="StrokeThickness" Value="1"/>
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="StrokeDashArray" Value="2 2"/>
        <Setter Property="Stretch" Value="Uniform"/>
        <Setter Property="Margin" Value="0,0,3,0"/>
    </Style>

    <!-- Title Text Style -->
    <Style TargetType="{x:Type TextBlock}" x:Key="TitleTextStyle">
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Margin" Value="0,0,3,0"/>
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- DATAGRID TOOLBAR                                                             -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <!-- Toolbar Button Style Base -->
    <Style x:Key="StackPanelButtonStyleBase" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
        <Setter Property="Height" Value="24"/>
        <Setter Property="Width" Value="24"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="2"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.MouseOver.Background}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Button.MouseOver.Border}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Pressed.Background}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Button.Pressed.Border}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Vertical Separator Style for Toolbar -->
    <Style x:Key="VerticalSeparatorStyle" TargetType="{x:Type Separator}">
        <Setter Property="Margin" Value="2,4,2,4"/>
        <Setter Property="Background" Value="{DynamicResource SeparatorBorder}"/>
        <Setter Property="LayoutTransform">
            <Setter.Value>
                <RotateTransform Angle="90"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DataGridToolbarStyle" TargetType="{x:Type local:DataGridToolbar}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BackgroundColor" Value="Transparent"/>
        <Setter Property="StackPanelButtonStyle" Value="{DynamicResource StackPanelButtonStyleBase}"/>
        <Setter Property="StackPanelSeparatorStyle" Value="{DynamicResource VerticalSeparatorStyle}"/>
    </Style>

    <Style TargetType="{x:Type local:DataGridToolbar}" BasedOn="{StaticResource DataGridToolbarStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- SEPARATOR WITH HEADER                                                        -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="SeparatorWithHeaderStyle" TargetType="{x:Type local:SeparatorWithHeader}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <Style TargetType="{x:Type local:SeparatorWithHeader}" BasedOn="{StaticResource SeparatorWithHeaderStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- NUMERIC TEXTBOX                                                              -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="NumericTextBoxStyle" TargetType="{x:Type local:NumericTextBox}">
        <Setter Property="Background" Value="{DynamicResource TextBox.Static.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource TextBox.Static.Border}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBox.Static.Foreground}"/>
    </Style>

    <Style TargetType="{x:Type local:NumericTextBox}" BasedOn="{StaticResource NumericTextBoxStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- NUMERIC TEXTBOX 2                                                            -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="NumericTextBox2Style" TargetType="{x:Type local:NumericTextBox2}">
        <Setter Property="Background" Value="{DynamicResource TextBox.Static.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource TextBox.Static.Border}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBox.Static.Foreground}"/>
    </Style>

    <Style TargetType="{x:Type local:NumericTextBox2}" BasedOn="{StaticResource NumericTextBox2Style}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- NAME TEXTBOX                                                                 -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="NameTextBoxStyle" TargetType="{x:Type local:NameTextBox}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <Style TargetType="{x:Type local:NameTextBox}" BasedOn="{StaticResource NameTextBoxStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- NUMERIC UP DOWN                                                              -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="NumericUpDownStyle" TargetType="{x:Type local:NumericUpDown}">
        <Setter Property="Background" Value="{DynamicResource TextBox.Static.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource TextBox.Static.Border}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBox.Static.Foreground}"/>
    </Style>

    <Style TargetType="{x:Type local:NumericUpDown}" BasedOn="{StaticResource NumericUpDownStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- NUMERIC SLIDER                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="NumericSliderStyle" TargetType="{x:Type local:NumericSlider}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <Style TargetType="{x:Type local:NumericSlider}" BasedOn="{StaticResource NumericSliderStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- THICKNESS CONTROL                                                            -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="ThicknessControlStyle" TargetType="{x:Type local:ThicknessControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <Style TargetType="{x:Type local:ThicknessControl}" BasedOn="{StaticResource ThicknessControlStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- GRID LENGTH CONTROL                                                          -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="GridLengthControlStyle" TargetType="{x:Type local:GridLengthControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <Style TargetType="{x:Type local:GridLengthControl}" BasedOn="{StaticResource GridLengthControlStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- COLOR PICKER                                                                 -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="ColorPickerStyle" TargetType="{x:Type local:ColorPicker}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
        <Setter Property="Background" Value="{DynamicResource EnvironmentWindowBackground}"/>
    </Style>

    <Style TargetType="{x:Type local:ColorPicker}" BasedOn="{StaticResource ColorPickerStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- COLOR PICKER POPUP                                                           -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="ColorPickerPopupStyle" TargetType="{x:Type local:ColorPickerPopup}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
        <Setter Property="Background" Value="{DynamicResource EnvironmentWindowBackground}"/>
    </Style>

    <Style TargetType="{x:Type local:ColorPickerPopup}" BasedOn="{StaticResource ColorPickerPopupStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- CLOCK CONTROL                                                                -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="ClockControlStyle" TargetType="{x:Type local:ClockControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <Style TargetType="{x:Type local:ClockControl}" BasedOn="{StaticResource ClockControlStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- DATE AND TIME CONTROLS                                                       -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="DateAndTimePickerControlStyle" TargetType="{x:Type local:DateAndTimePickerControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <Style TargetType="{x:Type local:DateAndTimePickerControl}" BasedOn="{StaticResource DateAndTimePickerControlStyle}"/>

    <Style x:Key="DateAndTimeTextBoxControlStyle" TargetType="{x:Type local:DateAndTimeTextBoxControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <Style TargetType="{x:Type local:DateAndTimeTextBoxControl}" BasedOn="{StaticResource DateAndTimeTextBoxControlStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- RESIZABLE TEXTBOX                                                            -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="ResizableTextBoxControlStyle" TargetType="{x:Type local:ResizableTextBoxControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <Style TargetType="{x:Type local:ResizableTextBoxControl}" BasedOn="{StaticResource ResizableTextBoxControlStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- NAME DIALOG                                                                  -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="NameDialogStyle" TargetType="{x:Type local:NameDialog}">
        <Setter Property="Background" Value="{DynamicResource EnvironmentWindowBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <Style TargetType="{x:Type local:NameDialog}" BasedOn="{StaticResource NameDialogStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- PROPERTY CONTROLS                                                            -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <!-- Base Property Control Style -->
    <Style x:Key="PropertyControlBaseStyle" TargetType="{x:Type UserControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>

    <!-- Text Property Control -->
    <Style x:Key="TextPropertyControlStyle" TargetType="{x:Type local:TextPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:TextPropertyControl}" BasedOn="{StaticResource TextPropertyControlStyle}"/>

    <!-- Name Text Property Control -->
    <Style x:Key="NameTextPropertyControlStyle" TargetType="{x:Type local:NameTextPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:NameTextPropertyControl}" BasedOn="{StaticResource NameTextPropertyControlStyle}"/>

    <!-- Resizeable Text Property Control -->
    <Style x:Key="ResizeableTextPropertyControlStyle" TargetType="{x:Type local:ResizeableTextPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:ResizeableTextPropertyControl}" BasedOn="{StaticResource ResizeableTextPropertyControlStyle}"/>

    <!-- Boolean Property Control -->
    <Style x:Key="BooleanPropertyControlStyle" TargetType="{x:Type local:BooleanPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:BooleanPropertyControl}" BasedOn="{StaticResource BooleanPropertyControlStyle}"/>

    <!-- Numeric Property Control -->
    <Style x:Key="NumericPropertyControlStyle" TargetType="{x:Type local:NumericPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:NumericPropertyControl}" BasedOn="{StaticResource NumericPropertyControlStyle}"/>

    <!-- Numeric Auto Property Control -->
    <Style x:Key="NumericAutoPropertyControlStyle" TargetType="{x:Type local:NumericAutoPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:NumericAutoPropertyControl}" BasedOn="{StaticResource NumericAutoPropertyControlStyle}"/>

    <!-- Numeric Slider Property Control -->
    <Style x:Key="NumericSliderPropertyControlStyle" TargetType="{x:Type local:NumericSliderPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:NumericSliderPropertyControl}" BasedOn="{StaticResource NumericSliderPropertyControlStyle}"/>

    <!-- Numeric Property Selector Control -->
    <Style x:Key="NumericPropertySelectorControlStyle" TargetType="{x:Type local:NumericPropertySelectorControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:NumericPropertySelectorControl}" BasedOn="{StaticResource NumericPropertySelectorControlStyle}"/>

    <!-- Color Property Control -->
    <Style x:Key="ColorPropertyControlStyle" TargetType="{x:Type local:ColorPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:ColorPropertyControl}" BasedOn="{StaticResource ColorPropertyControlStyle}"/>

    <!-- DateTime Property Control -->
    <Style x:Key="DateTimePropertyControlStyle" TargetType="{x:Type local:DateTimePropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:DateTimePropertyControl}" BasedOn="{StaticResource DateTimePropertyControlStyle}"/>

    <!-- Font Selector Control -->
    <Style x:Key="FontSelectorControlStyle" TargetType="{x:Type local:FontSelectorControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:FontSelectorControl}" BasedOn="{StaticResource FontSelectorControlStyle}"/>

    <!-- Font Weight Selector Control -->
    <Style x:Key="FontWeightSelectorControlStyle" TargetType="{x:Type local:FontWeightSelectorControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:FontWeightSelectorControl}" BasedOn="{StaticResource FontWeightSelectorControlStyle}"/>

    <!-- Line Style Selector Control -->
    <Style x:Key="LineStyleSelectorControlStyle" TargetType="{x:Type local:LineStyleSelectorControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:LineStyleSelectorControl}" BasedOn="{StaticResource LineStyleSelectorControlStyle}"/>

    <!-- Line Width Selector Control -->
    <Style x:Key="LineWidthSelectorControlStyle" TargetType="{x:Type local:LineWidthSelectorControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:LineWidthSelectorControl}" BasedOn="{StaticResource LineWidthSelectorControlStyle}"/>

    <!-- Horizontal Alignment Control -->
    <Style x:Key="HorizontalAlignmentControlStyle" TargetType="{x:Type local:HorizontalAlignmentControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:HorizontalAlignmentControl}" BasedOn="{StaticResource HorizontalAlignmentControlStyle}"/>

    <!-- Vertical Alignment Control -->
    <Style x:Key="VerticalAlignmentControlStyle" TargetType="{x:Type local:VerticalAlignmentControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:VerticalAlignmentControl}" BasedOn="{StaticResource VerticalAlignmentControlStyle}"/>

    <!-- Content Property Control -->
    <Style x:Key="ContentPropertyControlStyle" TargetType="{x:Type local:ContentPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:ContentPropertyControl}" BasedOn="{StaticResource ContentPropertyControlStyle}"/>

    <!-- Point Property Control -->
    <Style x:Key="PointPropertyControlStyle" TargetType="{x:Type local:PointPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:PointPropertyControl}" BasedOn="{StaticResource PointPropertyControlStyle}"/>

    <!-- Point3D Property Control -->
    <Style x:Key="Point3DPropertyControlStyle" TargetType="{x:Type local:Point3DPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:Point3DPropertyControl}" BasedOn="{StaticResource Point3DPropertyControlStyle}"/>

    <!-- String List Property Control -->
    <Style x:Key="StringListPropertyControlStyle" TargetType="{x:Type local:StringListPropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:StringListPropertyControl}" BasedOn="{StaticResource StringListPropertyControlStyle}"/>

    <!-- File Selector Control -->
    <Style x:Key="FileSelectorControlStyle" TargetType="{x:Type local:FileSelectorControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:FileSelectorControl}" BasedOn="{StaticResource FileSelectorControlStyle}"/>

    <!-- Folder Selector Control -->
    <Style x:Key="FolderSelectorControlStyle" TargetType="{x:Type local:FolderSelectorControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:FolderSelectorControl}" BasedOn="{StaticResource FolderSelectorControlStyle}"/>

    <!-- Directory Selector Control -->
    <Style x:Key="DirectorySelectorControlStyle" TargetType="{x:Type local:DirectorySelectorControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:DirectorySelectorControl}" BasedOn="{StaticResource DirectorySelectorControlStyle}"/>

    <!-- Calendar Week Rule Property Control -->
    <Style x:Key="CalendarWeekRulePropertyControlStyle" TargetType="{x:Type local:CalendarWeekRulePropertyControl}">
        <Setter Property="Foreground" Value="{DynamicResource EnvironmentWindowText}"/>
    </Style>
    <Style TargetType="{x:Type local:CalendarWeekRulePropertyControl}" BasedOn="{StaticResource CalendarWeekRulePropertyControlStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- DATAGRID CELL STYLES (Backward Compatible)                                   -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <!-- Left-aligned Cell Style -->
    <Style x:Key="Left_CellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="TextBlock.TextAlignment" Value="Left"/>
    </Style>

    <!-- Center-aligned Cell Style -->
    <Style x:Key="Center_CellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="TextBlock.TextAlignment" Value="Center"/>
    </Style>

    <!-- Center-aligned Read-Only Cell Style -->
    <Style x:Key="Center_ReadOnly_CellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="TextBlock.TextAlignment" Value="Center"/>
        <Setter Property="TextBlock.Background" Value="{DynamicResource EnvironmentWindowBackground}"/>
    </Style>

    <!-- Right-aligned Cell Style -->
    <Style x:Key="Right_CellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="TextBlock.TextAlignment" Value="Right"/>
    </Style>

    <!-- Disabled Cell Style -->
    <Style x:Key="Disabled_CellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="TextBlock.TextAlignment" Value="Center"/>
        <Setter Property="IsEnabled" Value="False"/>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- DATAGRID COLUMN AND ROW HEADER STYLES                                        -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <!-- Column Header Gripper Style for resizing columns -->
    <Style x:Key="ColumnHeaderGripperStyle" TargetType="{x:Type Thumb}">
        <Setter Property="Width" Value="2"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Cursor" Value="SizeWE"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border Background="Transparent" Padding="{TemplateBinding Padding}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Row Header Gripper Style for resizing rows -->
    <Style x:Key="RowHeaderGripperStyle" TargetType="{x:Type Thumb}">
        <Setter Property="Height" Value="2"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Cursor" Value="SizeNS"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border Background="Transparent" Padding="{TemplateBinding Padding}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Theme-aware Column Header Style with gradient background and hover effects -->
    <Style x:Key="ColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap"
                               TextAlignment="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType=DataGridColumnHeader}, Converter={x:Static local:HorizontalAlignmentToTextAlignmentConverter.Instance}}"
                               VerticalAlignment="Center"
                               FontWeight="Bold"
                               HorizontalAlignment="Stretch"
                               Text="{Binding}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="Foreground" Value="{DynamicResource DataGrid.Header.Foreground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DataGrid.Header.Border}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="6,3,6,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                    <Grid>
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <Grid Name="HeaderGrid" SnapsToDevicePixels="True" Margin="1,0,0,0">
                                <Grid.Background>
                                    <LinearGradientBrush StartPoint="0 0" EndPoint="0 1">
                                        <LinearGradientBrush.GradientStops>
                                            <GradientStop Offset="0.25" Color="{DynamicResource DataGrid.Header.Background}"/>
                                            <GradientStop Offset="1" Color="{DynamicResource DataGrid.Header.GradientEnd}"/>
                                        </LinearGradientBrush.GradientStops>
                                    </LinearGradientBrush>
                                </Grid.Background>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Margin="{TemplateBinding Padding}" Grid.Column="0" SnapsToDevicePixels="True"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                <Path x:Name="SortArrow" Visibility="Collapsed" Data="M0,0 L1,0 0.5,1 z" Stretch="Fill"
                                      Grid.Column="1" Width="8" Height="6" Fill="{DynamicResource DataGrid.Header.Foreground}" Margin="0,0,8,0"
                                      VerticalAlignment="Center" RenderTransformOrigin="0.5,0.4"/>
                            </Grid>
                        </Border>
                        <Thumb x:Name="PART_LeftHeaderGripper" HorizontalAlignment="Left" Style="{StaticResource ColumnHeaderGripperStyle}"/>
                        <Thumb x:Name="PART_RightHeaderGripper" HorizontalAlignment="Right" Style="{StaticResource ColumnHeaderGripperStyle}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="SortArrow" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Descending">
                            <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="HeaderGrid" Value="{DynamicResource DataGrid.Header.MouseOver.Background}"/>
                            <Setter TargetName="HeaderGrid" Property="Margin" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="Background" TargetName="HeaderGrid" Value="{DynamicResource DataGrid.Header.MouseOver.Background}"/>
                            <Setter TargetName="HeaderGrid" Property="Margin" Value="0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Default style for DataGridColumnHeader -->
    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource ColumnHeaderStyle}"/>

    <!-- Left-aligned Column Header Style -->
    <Style x:Key="LeftAlignedColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource ColumnHeaderStyle}">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
    </Style>

    <!-- Right-aligned Column Header Style -->
    <Style x:Key="RightAlignedColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource ColumnHeaderStyle}">
        <Setter Property="HorizontalContentAlignment" Value="Right"/>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- BACKWARD COMPATIBLE COLUMN HEADER STYLES (Bold)                              -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <!-- Left-aligned Column Header Style (Bold) -->
    <Style x:Key="Left_ColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource ColumnHeaderStyle}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Center"
                               FontWeight="Bold" HorizontalAlignment="Stretch" Text="{Binding}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Center-aligned Column Header Style (Bold) -->
    <Style x:Key="Center_ColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource ColumnHeaderStyle}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Center"
                               FontWeight="Bold" HorizontalAlignment="Stretch" Text="{Binding}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Right-aligned Column Header Style (Bold) -->
    <Style x:Key="Right_ColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource ColumnHeaderStyle}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap" TextAlignment="Right" VerticalAlignment="Center"
                               FontWeight="Bold" HorizontalAlignment="Stretch" Text="{Binding}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- MODAL COLUMN HEADER STYLES (Normal Weight)                                   -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <!-- Left-aligned Modal Column Header Style (Normal Weight) -->
    <Style x:Key="Left_ColumnHeaderStyle_Modal" TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource ColumnHeaderStyle}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Center"
                               FontWeight="Normal" HorizontalAlignment="Stretch" Text="{Binding}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Center-aligned Modal Column Header Style (Normal Weight) -->
    <Style x:Key="Center_ColumnHeaderStyle_Modal" TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource ColumnHeaderStyle}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Center"
                               FontWeight="Normal" HorizontalAlignment="Stretch" Text="{Binding}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Right-aligned Modal Column Header Style (Normal Weight) -->
    <Style x:Key="Right_ColumnHeaderStyle_Modal" TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource ColumnHeaderStyle}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap" TextAlignment="Right" VerticalAlignment="Center"
                               FontWeight="Normal" HorizontalAlignment="Stretch" Text="{Binding}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- ROW HEADER STYLES                                                            -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <!-- Theme-aware Row Header Style (flat, no gradient to match row items) -->
    <Style x:Key="RowHeaderStyle" TargetType="{x:Type DataGridRowHeader}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap"
                               TextAlignment="Center"
                               VerticalAlignment="Center"
                               FontWeight="Bold"
                               HorizontalAlignment="Stretch"
                               Text="{Binding}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Height" Value="22"/>
        <Setter Property="Foreground" Value="{DynamicResource DataGrid.Header.Foreground}"/>
        <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DataGrid.Header.Border}"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="6,3,6,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridRowHeader}">
                    <Grid>
                        <Border Name="HeaderBorder"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}">
                            <Grid Name="HeaderGrid" SnapsToDevicePixels="True" Margin="1,0,0,0">
                                <Grid.Background>
                                    <LinearGradientBrush StartPoint="0 0" EndPoint="0 1">
                                        <LinearGradientBrush.GradientStops>
                                            <GradientStop Offset="0.25" Color="{DynamicResource DataGrid.Header.Background}"/>
                                            <GradientStop Offset="1" Color="{DynamicResource DataGrid.Header.GradientEnd}"/>
                                        </LinearGradientBrush.GradientStops>
                                    </LinearGradientBrush>
                                </Grid.Background>
                                <ContentPresenter Margin="{TemplateBinding Padding}" SnapsToDevicePixels="True"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Grid>
                        </Border>
                        <Thumb x:Name="PART_TopHeaderGripper" VerticalAlignment="Top" Style="{StaticResource RowHeaderGripperStyle}"/>
                        <Thumb x:Name="PART_BottomHeaderGripper" VerticalAlignment="Bottom" Style="{StaticResource RowHeaderGripperStyle}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="HeaderGrid" Value="{DynamicResource DataGrid.Header.MouseOver.Background}"/>
                            <Setter TargetName="HeaderGrid" Property="Margin" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="Background" TargetName="HeaderGrid" Value="{DynamicResource DataGrid.Header.MouseOver.Background}"/>
                            <Setter TargetName="HeaderGrid" Property="Margin" Value="0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Default style for DataGridRowHeader -->
    <Style TargetType="{x:Type DataGridRowHeader}" BasedOn="{StaticResource RowHeaderStyle}"/>

    <!-- Right-aligned Row Header Style -->
    <Style x:Key="Right_RowHeaderStyle" TargetType="{x:Type DataGridRowHeader}" BasedOn="{StaticResource RowHeaderStyle}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap" TextAlignment="Right" VerticalAlignment="Center"
                               FontWeight="Bold" HorizontalAlignment="Stretch" Text="{Binding}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- COPY PASTE DATAGRID                                                          -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <!-- DataGrid Row Style for CopyPasteDataGrid -->
    <Style x:Key="CopyPasteDataGridRowStyle" TargetType="{x:Type DataGridRow}">
        <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource DataGrid.Row.Foreground}"/>
        <Style.Triggers>
            <Trigger Property="AlternationIndex" Value="1">
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Alternating.Background}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.MouseOver.Background}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Selection.Background}"/>
                <Setter Property="Foreground" Value="{DynamicResource DataGrid.Row.Selection.Foreground}"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True"/>
                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Selection.Inactive.Background}"/>
                <Setter Property="Foreground" Value="{DynamicResource DataGrid.Row.Selection.Inactive.Foreground}"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!-- DataGrid Cell Style for CopyPasteDataGrid -->
    <Style x:Key="CopyPasteDataGridCellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=DataGridRow}}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <!-- Cell selection highlighting for extended selection mode -->
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Selection.Background}"/>
                <Setter Property="Foreground" Value="{DynamicResource DataGrid.Row.Selection.Foreground}"/>
            </Trigger>
            <!-- Inactive selection (when DataGrid loses focus) -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True"/>
                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Selection.Inactive.Background}"/>
                <Setter Property="Foreground" Value="{DynamicResource DataGrid.Row.Selection.Inactive.Foreground}"/>
            </MultiTrigger>
            <!-- Focus border for keyboard navigation -->
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource DataGrid.Cell.Focus.Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CopyPasteDataGridStyle" TargetType="{x:Type local:CopyPasteDataGrid}">
        <Setter Property="Background" Value="{DynamicResource EnvironmentWindowBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DataGrid.Static.Border}"/>
        <Setter Property="Foreground" Value="{DynamicResource DataGrid.Static.Foreground}"/>
        <Setter Property="RowBackground" Value="{DynamicResource DataGrid.Row.Background}"/>
        <Setter Property="AlternatingRowBackground" Value="{DynamicResource DataGrid.Row.Alternating.Background}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource DataGrid.GridLines}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource DataGrid.GridLines}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="AlternationCount" Value="2"/>
        <Setter Property="RowHeight" Value="22"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="GridLinesVisibility" Value="All"/>
        <Setter Property="RowStyle" Value="{StaticResource CopyPasteDataGridRowStyle}"/>
        <Setter Property="CellStyle" Value="{StaticResource CopyPasteDataGridCellStyle}"/>
        <Setter Property="ColumnHeaderStyle" Value="{DynamicResource ColumnHeaderStyle}"/>
        <Setter Property="RowHeaderStyle" Value="{DynamicResource RowHeaderStyle}"/>
    </Style>

    <Style TargetType="{x:Type local:CopyPasteDataGrid}" BasedOn="{StaticResource CopyPasteDataGridStyle}"/>

    <!-- ═══════════════════════════════════════════════════════════════════════════ -->
    <!-- VALIDATION DATAGRID                                                          -->
    <!-- ═══════════════════════════════════════════════════════════════════════════ -->

    <!-- DataGrid Row Style for ValidationDataGrid -->
    <Style x:Key="ValidationDataGridRowStyle" TargetType="{x:Type DataGridRow}">
        <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource DataGrid.Row.Foreground}"/>
        <Style.Triggers>
            <Trigger Property="AlternationIndex" Value="1">
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Alternating.Background}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.MouseOver.Background}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Selection.Background}"/>
                <Setter Property="Foreground" Value="{DynamicResource DataGrid.Row.Selection.Foreground}"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True"/>
                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Selection.Inactive.Background}"/>
                <Setter Property="Foreground" Value="{DynamicResource DataGrid.Row.Selection.Inactive.Foreground}"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!-- DataGrid Cell Style for ValidationDataGrid (includes error handling) -->
    <Style x:Key="ValidationDataGridCellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=DataGridRow}}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <!-- Cell selection highlighting for extended selection mode -->
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Selection.Background}"/>
                <Setter Property="Foreground" Value="{DynamicResource DataGrid.Row.Selection.Foreground}"/>
            </Trigger>
            <!-- Inactive selection (when DataGrid loses focus) -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True"/>
                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource DataGrid.Row.Selection.Inactive.Background}"/>
                <Setter Property="Foreground" Value="{DynamicResource DataGrid.Row.Selection.Inactive.Foreground}"/>
            </MultiTrigger>
            <!-- Focus border for keyboard navigation -->
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource DataGrid.Cell.Focus.Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ValidationDataGridStyle" TargetType="{x:Type local:ValidationDataGrid}">
        <Setter Property="Background" Value="{DynamicResource EnvironmentWindowBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DataGrid.Static.Border}"/>
        <Setter Property="Foreground" Value="{DynamicResource DataGrid.Static.Foreground}"/>
        <Setter Property="RowBackground" Value="{DynamicResource DataGrid.Row.Background}"/>
        <Setter Property="AlternatingRowBackground" Value="{DynamicResource DataGrid.Row.Alternating.Background}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource DataGrid.GridLines}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource DataGrid.GridLines}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="AlternationCount" Value="2"/>
        <Setter Property="RowHeight" Value="22"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="GridLinesVisibility" Value="All"/>
        <Setter Property="RowStyle" Value="{StaticResource ValidationDataGridRowStyle}"/>
        <Setter Property="CellStyle" Value="{StaticResource ValidationDataGridCellStyle}"/>
        <Setter Property="ColumnHeaderStyle" Value="{DynamicResource ColumnHeaderStyle}"/>
        <Setter Property="RowHeaderStyle" Value="{DynamicResource RowHeaderStyle}"/>
        <!-- Theme-aware error colors -->
        <Setter Property="ErrorCellBorderBrush" Value="{DynamicResource DataGrid.Cell.Error.Border}"/>
        <Setter Property="ErrorCellBackgroundBrush" Value="{DynamicResource DataGrid.Cell.Error.Background}"/>
    </Style>

    <Style TargetType="{x:Type local:ValidationDataGrid}" BasedOn="{StaticResource ValidationDataGridStyle}"/>

</ResourceDictionary>
