<UserControl x:Class="GenericControls.DataGridToolbar" x:Name="DataGridToolsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GenericControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">
    <UserControl.Resources>
        <ResourceDictionary Source="../Resources/ResourceDictionary.xaml"/>
    </UserControl.Resources>
    <StackPanel Name="ToolbarStackPanel" Orientation="{Binding ElementName=DataGridToolsControl, Path=ToolOrientation, TargetNullValue=Horizontal, FallbackValue=Horizontal}" Background="{Binding ElementName=DataGridToolsControl, Path=BackgroundColor}">
        <!--Add Row(s)-->
        <Button x:Name="AddRowsButton" ToolTip="Add row(s) to the bottom of the table" Click="AddRowsButton_Click">
            <ContentControl Content="{DynamicResource AddRowIcon}"/>
        </Button>
        <!--Insert Row(s)-->
        <Button x:Name="InsertRowsButton" ToolTip="Insert row(s) above selected cell(s) in table" Click="InsertRowsButton_Click">
            <ContentControl Content="{DynamicResource InsertRowIcon}"/>
        </Button>
        <!--Delete Row(s)-->
        <Button x:Name="DeleteRowsButton" ToolTip="Delete row(s) from the table" Click="DeleteRowsButton_Click">
            <ContentControl Content="{DynamicResource DeleteRowIcon}"/>
        </Button>

        <!--Separator-->
        <Separator x:Name="CustomOptionsSeparator" Visibility="Collapsed"/>
        <!--Custom Buttons-->


        <!--Separator-->
        <Separator x:Name="EditSelectSeparator" />
        <!--Select All-->
        <Button x:Name="SelectAllButton" ToolTip="Select all cells" Click="SelectAllButton_Click">
            <ContentControl Content="{DynamicResource SelectAllIcon}"/>
        </Button>

        <!--Copy-->
        <Button x:Name="CopyButton" ToolTip="Copy selected cells" Click="CopyButton_Click">
            <ContentControl Content="{DynamicResource CopyIcon}"/>
        </Button>

        <!--Copy with Headers-->
        <Button x:Name="CopyWithHeadersButton" ToolTip="Copy selected cells with table headers" Click="CopyWithHeadersButton_Click">
            <ContentControl Content="{DynamicResource CopyWithHeadersIcon}"/>
        </Button>

        <!--Paste-->
        <Button x:Name="PasteButton" ToolTip="Paste from the clipboard into the table" Click="PasteButton_Click">
            <ContentControl Content="{DynamicResource PasteIcon}"/>
        </Button>

    </StackPanel>
</UserControl>
